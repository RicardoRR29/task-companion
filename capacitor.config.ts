import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.taco',
  appName: 'TACO – Task Companion',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
